{
  "version": "0.2.0",
  "configurations": [
    {
      // Launches the extension inside a new window
      "name": "Extension",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "npm:vscode:prepublish",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"]
    }
  ]
}
